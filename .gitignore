node_modules/
.expo/
dist/
.jury-check/
web-build/
expo-env.d.ts
*.log
npm-debug.*
.DS_Store
*.jks
*.p8
*.p12
*.key
*.mobileprovision
*.orig.*

# Supabase CLI scratch state (created by `supabase start` / `supabase link`)
supabase/.temp/
supabase/.branches/

# Local secrets. EXPO_PUBLIC_* values are public by design (RLS guards the data),
# but they are environment-specific: document them in README, not in git history.
.env
